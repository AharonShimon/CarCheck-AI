{
  "name": "carcheck-ai-pro",
  "version": "1.0.0",
  "description": "מערכת בדיקת רכב משולבת AI ומאגרי ממשלה",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "@google/generative-ai": "^0.11.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

